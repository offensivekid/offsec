# 🔐 offensive-forum v3.0 - SECURE EDITION

Полностью защищённый форум с SIEM мониторингом, продвинутой админ-панелью и профессиональными мерами безопасности.

## 🛡️ Реализованная Безопасность

### Аутентификация и Авторизация
- ✅ bcrypt хеширование паролей (12 раундов)
- ✅ Защита от brute-force (5 попыток за 15 минут)
- ✅ Безопасные сессии с httpOnly cookies
- ✅ CSRF защита через SameSite cookies
- ✅ Автоматическая блокировка аккаунта после failed attempts

### Безопасность Сервера
- ✅ Helmet.js для HTTP заголовков безопасности
- ✅ Content Security Policy (CSP)
- ✅ HSTS для принудительного HTTPS
- ✅ Rate limiting (глобальный и для auth эндпоинтов)
- ✅ IP banning система
- ✅ XSS защита через sanitization
- ✅ SQL injection защита (prepared statements)

### SIEM и Мониторинг
- ✅ Полное логирование всех событий
- ✅ 4 уровня severity: critical, high, medium, low
- ✅ Логирование IP, User-Agent, endpoints
- ✅ Детализированная информация о событиях
- ✅ Автоматическая очистка старых логов
- ✅ Фильтрация по severity и event type

### Админ-Панель
- ✅ Управление пользователями (создание, бан, удаление, роли)
- ✅ Модерация контента (удаление тредов и реплаев)
- ✅ Генерация и управление access keys
- ✅ SIEM dashboard с live мониторингом
- ✅ IP ban management
- ✅ Смена пароля
- ✅ Детальная статистика

## 🚀 Быстрый Старт

### 1. Установка

```bash
git clone <your-repo>
cd offensive-forum
npm install
```

### 2. Конфигурация

```bash
cp .env.example .env
nano .env  # Установите SESSION_SECRET и ADMIN_PASSWORD
```

**ВАЖНО:** Сгенерируйте безопасный SESSION_SECRET:
```bash
openssl rand -base64 32
```

### 3. Запуск

```bash
npm start
```

При первом запуске:
- Создастся база данных
- Создастся админ с указанным паролем
- Сгенерируется sample access key
- Выведутся credentials админа

### 4. Доступ

- **Форум:** http://localhost:8080
- **Админ-панель:** http://localhost:8080/admin

## 🔧 Деплой на Railway

### Метод 1: Через GitHub

1. Запушьте код на GitHub
2. Подключите репозиторий в Railway
3. Добавьте переменные окружения:
   ```
   SESSION_SECRET=ваш-случайный-секрет-32-символа
   ADMIN_PASSWORD=ваш-безопасный-пароль
   NODE_ENV=production
   ```
4. Deploy!

### Метод 2: Railway CLI

```bash
railway login
railway init
railway up
railway variables set SESSION_SECRET=<your-secret>
railway variables set ADMIN_PASSWORD=<your-password>
```

## 📊 Структура Проекта

```
offensive-forum/
├── server.js              # Основной сервер (1000+ строк)
├── package.json
├── .env.example
├── admin/
│   ├── index.html        # Админ-панель UI
│   └── admin.js          # Админ-панель логика
├── public/
│   ├── index.html        # Публичный форум
│   ├── app.js           # Фронтенд логика
│   └── styles.css       # Стили
└── database.sqlite       # SQLite база (создаётся автоматически)
```

## 🔐 Безопасность Best Practices

### Что УЖЕ сделано:
1. ✅ Нет хардкод паролей (генерируется случайно или из .env)
2. ✅ Админка на отдельном роуте с авторизацией
3. ✅ Все API endpoints защищены middleware
4. ✅ Prepared statements против SQL injection
5. ✅ Sanitization всех user inputs
6. ✅ Rate limiting против DDoS
7. ✅ SIEM для детекции аномалий
8. ✅ IP banning для блокировки атакующих

### Что НУЖНО сделать:
1. ⚠️ Измените SESSION_SECRET после деплоя
2. ⚠️ Измените пароль админа через админ-панель
3. ⚠️ Включите HTTPS в production (Railway делает автоматически)
4. ⚠️ Регулярно чекайте SIEM логи на подозрительную активность
5. ⚠️ Настройте backup базы данных

## 🎯 SIEM Event Types

### Critical Severity
- `banned_ip_attempt` - Попытка доступа с забаненного IP
- IP ban/unban операции

### High Severity
- `admin_access_denied` - Попытка доступа к админке без прав
- `banned_login_attempt` - Попытка логина забаненного юзера
- `account_locked` - Аккаунт заблокирован после failed attempts
- `user_ban_changed` - Изменение бан статуса
- `user_role_changed` - Изменение ролей
- `user_deleted` - Удаление юзера
- `auth_rate_limit` - Превышен лимит попыток логина

### Medium Severity
- `login_failed` - Неудачный логин
- `unauthorized_access` - Попытка доступа без авторизации
- `unauthorized_private_thread` - Попытка создать private thread без прав
- `password_change_failed` - Неудачная смена пароля
- `password_changed` - Успешная смена пароля
- `rate_limit_exceeded` - Превышен rate limit
- `user_created_by_admin` - Админ создал юзера
- Thread/reply deletion

### Low Severity
- `login_success` - Успешный логин
- `logout` - Выход
- `user_registered` - Регистрация
- `thread_created` - Создание треда
- `reply_created` - Создание реплая
- `keys_generated` - Генерация ключей

## 🔍 Мониторинг и Детекция

### Что смотреть в SIEM:

1. **Подозрительные паттерны:**
   - Много `login_failed` с одного IP → возможен brute-force
   - `unauthorized_access` к админке → кто-то ищет уязвимости
   - Множество `rate_limit_exceeded` → DDoS атака

2. **Аномалии:**
   - Логины в необычное время
   - Создание множества аккаунтов с одного IP
   - Массовое создание тредов/реплаев (спам)

3. **Критические события:**
   - Любые `critical` severity events
   - Попытки доступа с забаненных IP
   - Изменения ролей админов

## 📈 Масштабирование

Текущая версия оптимизирована для:
- До 10,000 пользователей
- До 100,000 тредов
- SQLite с WAL mode для лучшей concurrency

Для больших нагрузок:
1. Миграция на PostgreSQL
2. Redis для сессий
3. Load balancer
4. Separate SIEM database

## 🆘 Troubleshooting

### "Admin access denied"
- Убедитесь что логинились как admin
- Проверьте в базе: `SELECT * FROM users WHERE is_admin = 1`

### "Session secret required"
- Установите SESSION_SECRET в .env или переменных окружения

### "Rate limit exceeded"
- Подождите 15 минут
- Или удалите sessions.sqlite для сброса

### Забыли пароль админа
```bash
# Удалите базу и перезапустите (ВСЕ ДАННЫЕ БУДУТ УДАЛЕНЫ!)
rm database.sqlite sessions.sqlite
npm start
```

## 📝 API Endpoints

### Public
- `POST /api/register` - Регистрация
- `POST /api/login` - Логин
- `POST /api/logout` - Выход
- `GET /api/threads` - Список тредов
- `GET /api/threads/:id` - Детали треда
- `POST /api/threads` - Создать тред (требует auth)
- `POST /api/threads/:id/reply` - Ответить (требует auth)

### Admin Only
- `GET /api/admin/stats` - Статистика
- `GET /api/admin/users` - Список юзеров
- `POST /api/admin/users` - Создать юзера
- `PUT /api/admin/users/:id/ban` - Бан/анбан
- `PUT /api/admin/users/:id/role` - Изменить роль
- `DELETE /api/admin/users/:id` - Удалить юзера
- `GET /api/admin/threads` - Все треды
- `DELETE /api/admin/threads/:id` - Удалить тред
- `DELETE /api/admin/replies/:id` - Удалить реплай
- `POST /api/admin/keys/generate` - Генерировать ключи
- `GET /api/admin/keys` - Список ключей
- `DELETE /api/admin/keys/:id` - Удалить ключ
- `GET /api/admin/siem` - SIEM логи
- `DELETE /api/admin/siem` - Очистить старые логи
- `GET /api/admin/ipbans` - Список IP банов
- `POST /api/admin/ipbans` - Забанить IP
- `DELETE /api/admin/ipbans/:id` - Разбанить IP
- `POST /api/admin/change-password` - Сменить пароль

## 🤝 Contributing

Для улучшения безопасности:
1. Fork репозиторий
2. Создайте feature branch
3. Commit изменения
4. Push и создайте PR

## 📄 License

MIT License - делайте что хотите, но без гарантий!

## ⚠️ Disclaimer

Этот форум создан для образовательных целей. Автор не несёт ответственности за использование в незаконных целях или утечки данных из-за неправильной конфигурации.

## 🎉 Готово!

Теперь у тебя есть:
- ✅ Безопасный форум без хардкод паролей
- ✅ Полноценная админ-панель
- ✅ SIEM мониторинг
- ✅ IP banning
- ✅ User management
- ✅ Content moderation
- ✅ Все best practices безопасности

**Задеплой, поменяй пароль админа, и начинай мониторить!** 🚀
